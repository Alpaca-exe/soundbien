name: Build Windows Exe

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Generate Changelog
      run: |
        $ErrorActionPreference = 'Stop'
        try {
          $PreviousTag = git describe --tags --abbrev=0 HEAD^
          Write-Host "Previous tag: $PreviousTag"
        } catch {
          Write-Host "No previous tag found, showing all history"
          $PreviousTag = $null
        }

        if ($PreviousTag) {
          $Range = "$PreviousTag..HEAD"
        } else {
          $Range = "HEAD"
        }
        
        $changelog = git log --pretty=format:"- [%h](https://github.com/${{ github.repository }}/commit/%H) %s" $Range
        [System.IO.File]::WriteAllText("RELEASE_NOTES.md", $changelog, [System.Text.Encoding]::UTF8)
      shell: powershell

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller
      run: pyinstaller main.spec

    - name: Install Inno Setup
      run: |
        choco install innosetup -y
      shell: powershell

    - name: Build Installer
      run: |
        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
      shell: powershell

    - name: Upload Exe Artifact
      uses: actions/upload-artifact@v4
      with:
        name: SoundboardApp-Exe
        path: dist/soundbien.exe

    - name: Upload Installer Artifact
      uses: actions/upload-artifact@v4
      with:
        name: SoundboardApp-Setup
        path: installer_output/soundbien-setup.exe

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          dist/soundbien.exe
          installer_output/soundbien-setup.exe
        body_path: RELEASE_NOTES.md
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
